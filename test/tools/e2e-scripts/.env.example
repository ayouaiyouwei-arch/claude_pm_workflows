# <项目> PM 自跑回归 · 环境变量样例
# 复制为 .env（不要 commit）· dotenv 自动加载 · 见自跑 README § 一
# -----------------------------------------------------------------------------

# === 环境切换 ===
# staging（默认 · HTTPS 灰度）/ local（本地 docker-compose）
PLAYWRIGHT_ENV=staging

# baseURL 显式覆盖（可选 · 留空则按 PLAYWRIGHT_ENV 自动选）
# ADMIN_BASE_URL=https://<灰度域名>

# 基线版本号（决定 reports/ 归档目录）
BASELINE_VERSION=<基线版本号>

# 单用例超时 ms（默认 300_000 = 5min · 真实 UI 增删改用例可拉到 600_000）
# PW_TIMEOUT_MS=300000

# === 角色账号 ===
# 默认值已写死在 fixtures/auth.ts（统一口令 <口令>）
# 只有在你需要切换灰度/正式账号 / 个人账号时 才用 env 覆盖
# 下面仅 1 行示例 · 实际角色按本项目 /init-project 或 /init-docs 填充

# PM_<角色1>_USER=<角色1>
# PM_<角色1>_PASSWORD=<口令>

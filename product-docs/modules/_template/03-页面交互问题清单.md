# M0X-<模块名> · 03 页面交互问题清单（FE-XXX · append-only）

> 🔧 模板。编号规则：`FE-<模块缩写>-<3位序号>`，全生命周期不复用。本表是用例 CSV `fe_ref` 的来源；**只记观察不下结论**，是否算缺陷由 Gate D2 / 差异台账裁决。
> "影响"列必须业务语言（P015）：用户/客户会看到什么、会卡在哪。

| 编号 | 页面 | 现象（观察到什么） | 影响（业务语言） | 严重度（高/中/低） | 发现日期 | 状态 | 去向（DIFF-XXX / 有意设计→01 / 挂起Q-XXX） |
|---|---|---|---|---|---|---|---|

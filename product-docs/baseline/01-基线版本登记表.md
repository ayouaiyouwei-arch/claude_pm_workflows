# 01 · 基线版本登记表

> 🔧 项目无关骨架版 · 起始为空，由 `publish-baseline` skill 在发布基线时追加。

## 一、当前生效基线

> 起始为空。`publish-baseline` skill 在发布首个基线时填入下表。

| 字段 | 值 |
|---|---|
| 基线编号 | `<例: B1.0.0>` |
| 建立日期 | `<YYYY-MM-DD>` |
| 代码快照 commit | `<SHA>`（`<commit 摘要>`） |
| 拉取分支 | `<分支名>` |

## 二、版本历史

> 起始为空。每次发布基线由 `publish-baseline` skill 追加一行（append-only）。

| 版本 | 日期 | 关联变更（CHG）| commit | 备注 |
|---|---|---|---|---|
| `<例: B1.0.0>` | `<YYYY-MM-DD>` | `<例: 首次基线建立（/init-project）>` | `<SHA>` | `<备注>` |

# 05 · 用例清单（本次需求子集 + 全量 snapshot 索引）

> 🔧 项目无关骨架版 · 示例均为占位 · 项目专属配置见 PROJECT-PROFILE.md

> 本文件由 4 段组成：
> - § 一：字段说明（要点）
> - § 一.5：**全量 snapshot 指引**（D2 升级新增）
> - § 二/三：本期"必过 / 回归"用例子集
> - § 四：**本期用例增删改登记**（D2 升级新增，**冲突治理事实源**）
>
> ⚠️ Codex 提交前需自跑 `auto:Y` 标的用例。

---

## 一、用例字段说明

字段含义见 `./test-cases-snapshot/_用例字段说明.md`（snapshot 内冻结副本）。本文件只列要点子集，便于快速定位。

| 字段 | 取值 |
|---|---|
| `case_id` | `<模块缩写>-<3 位序号>`，例 `<MOD-001>`（缩写表见 `_用例字段说明.md § 四`） |
| `priority` | `P0 / P1 / P2`（30 : 50 : 20 配比建议） |
| `auto` | `Y / N`（是否有自动化资产） |
| `asset_path` | 自动化用例路径（Bruno `.bru` 或 Playwright `.spec.ts`），位于 `test/tools/`（包外，可读不可改） |
| `evidence_required` | `screenshot / har / sql / log` 任意组合 |

---

## 一.5、全量 snapshot 指引（D2 升级 · 关键）

### 1) snapshot 在哪

```
./test-cases-snapshot/
├── README.md                    ← 必读：说明 snapshot 与本文件的关系
├── _快照元数据.md                ← 必读：起包时间 + commit + SHA 校验
├── _用例字段说明.md              ← 字段语义冻结副本
├── _测试设计方法.md              ← 6 大方法论 + scenario 句式约束
└── <各模块>.csv                  ← 全量用例元数据（起包瞬间冻结，每模块一份）
```

### 2) 优先级（事实源）

```
05-用例清单.md § 四（本期增删改登记）
            ↓ 覆盖
05-用例清单.md § 二/三（本期必过 + 回归）
            ↓ 覆盖
test-cases-snapshot/<模块>.csv（全量起包时冻结）
            ↓ 仅参考
test/test-cases/<模块>.csv（包外上游，不读，避免污染）
```

### 3) 冲突治理铁律

> ⚠️ **新功能可能作废老用例。绝对不要同时按 snapshot 与本期新版做。**

| 当 case_id 出现在 § 四，标 `已作废` | snapshot 里的对应行**完全失效** |
| 当 case_id 出现在 § 四，标 `本期修改` | 只看 § 二/三 的最新版 |
| 当 case_id 出现在 § 四，标 `本期新增` | 只看 § 二/三（snapshot 里没有） |
| 当 case_id **未** 出现在 § 四 | snapshot 即最新版 |

---

## 二、本次必过用例

> 下表为占位示例（case_id / 标题 / asset_path 均为占位），演示"列表 / 过滤 / 抽屉 / 提交 / 幂等 / 越权 / partial / 性能"八类典型用例的写法，请替换为本项目真实模块用例。

| case_id | 标题 | priority | auto | asset_path | evidence_required |
|---|---|---|---|---|---|
| `<MOD-001>` | 进入<列表页>，默认 24h 倒序，5-state 全覆盖 | P0 | Y | `test/tools/e2e-scripts/tests/regression/<模块>/list.spec.ts` | screenshot+har |
| `<MOD-002>` | 列表过滤：`<类型字段>`多选 + 时间范围 | P0 | Y | `test/tools/e2e-scripts/.../filter.spec.ts` | screenshot+har |
| `<MOD-003>` | 点击记录 → 抽屉打开 → 主内容区可交互 | P0 | Y | `test/tools/e2e-scripts/.../detail-drawer.spec.ts` | screenshot |
| `<MOD-004>` | 提交结论 `<result_a>` → 列表状态刷新 | P0 | Y | `test/tools/e2e-scripts/.../submit-result.spec.ts` | screenshot+har+sql |
| `<MOD-005>` | 重复提交结论（同 X-Request-Id）幂等 | P0 | Y | `test/tools/api-collection/<模块>/submit-result-idempotent.bru` | har+log |
| `<MOD-006>` | 越权：`<只读角色>`提交结论返回 403 | P1 | Y | `test/tools/api-collection/<模块>/submit-result-rbac.bru` | har |
| `<MOD-007>` | 手动验收：抽屉主内容区加载失败 → 元信息仍可见（partial 状态） | P1 | N | 手动 + 浏览器 MCP 截屏 | screenshot |
| `<MOD-008>` | 性能：列表首屏 ≤ 2s（Network 慢 3G） | P2 | N | 手动 + Lighthouse | screenshot |

---

## 三、回归用例（防止本次改动伤其他功能）

> 下表为占位示例，回归用例通常覆盖"首页加载 / 登录跳转 / 菜单可达"等全局功能，请按本项目实际填写。

| case_id | 标题 | priority | 关联模块 |
|---|---|---|---|
| `<TC-XXX-001>` | `<首页加载正常>` | P1 | `<某模块>` |
| `<TC-XXX-002>` | 登录后跳转默认首页 | P1 | `<用户与权限>` |
| `<TC-XXX-003>` | 一级菜单全部可访问 | P0 | 全局 |

---

## 四、本期用例增删改登记（D2 升级 · 冲突治理事实源）

> ⚠️ **本节是覆盖 snapshot 的最高事实源**。每一条本期对老用例的处置都必须登记到这里，避免 Codex 同时按新老两版做。
>
> Codex / 测试 AI 读 snapshot 之前**必须**先扫这一节。

### 4.1 已作废（snapshot 里有，但本期需求让它失效）

| case_id | 原断言摘要 | 作废原因 | 替代 case_id（如有） |
|---|---|---|---|
| `<MOD-XYZ>` | `<原：导出列表为 Excel 文件>` | `<本期需求 § 七排除"导出"，该功能取消>` | — |
| `<...>` | `<...>` | `<...>` | `<...>` |

> 处置：snapshot 里这些行**完全失效**。Codex 不要为这些 case 写实现，不要写测试断言；测试不要执行。

### 4.2 本期修改（snapshot 与新版断言不同）

| case_id | snapshot 旧断言 | 新断言（本期） | 修改原因 |
|---|---|---|---|
| `<MOD-ABC>` | `<旧：抽屉宽度可拖动 30~80%>` | `<新：固定 60% 不可拖>` | `<03-PRD片段.md § 五已明确>` |
| `<...>` | `<...>` | `<...>` | `<...>` |

> 处置：以本节"新断言"为准，写代码 / 写自动化时按新断言；snapshot 里的旧断言仅作历史参考。

### 4.3 本期新增（snapshot 里没有，本期完全新写）

| case_id | 标题 | 已在 § 二/三 哪一行 |
|---|---|---|
| `<MOD-001>` | `<进入列表页，5-state 全覆盖>` | § 二 第 1 行 |
| `<MOD-002>` | `<...>` | § 二 第 2 行 |
| `<...>` | `<...>` | `<...>` |

> 处置：以 § 二/三 表格为唯一来源；snapshot 里没有这些 case_id 是预期的。

### 4.4 不变（snapshot 即最新，本期未触碰）

> 默认状态，不需要在本节列出。Codex 读 snapshot 即可。

### 4.5 冲突识别命令（PM 起包时自检）

```bash
# 检查 § 二 / § 三 列的 case_id 是否都在 snapshot 里能找到
for cid in <case_id 列表>; do
  grep -l "^$cid," test-cases-snapshot/*.csv || echo "$cid 在 snapshot 里没找到（应在 § 4.3 登记）"
done
```

---

## 五、测试资产位置（统一）

> ⚠️ Codex 对下表"包外"路径**可读 + 可执行（跑测试）**，但**不可改**。要改请走另起 CHG。
> 详见 `01-需求范围与边界.md § 五 读权限白名单` 与 `AGENTS.md § 二 铁律 1.2`。

| 类型 | 根目录 | 包内 vs 包外 |
|---|---|---|
| CSV 用例（**全量冻结快照**） | `./test-cases-snapshot/` | **包内** |
| CSV 用例（上游事实源） | `test/test-cases/<模块>.csv` | 包外（PM 维护，Codex **不读**） |
| Bruno 集合 | `test/tools/api-collection/<模块>/` | 包外（可读 + 可执行 + **不可改**） |
| Playwright | `test/tools/e2e-scripts/tests/regression/<模块>/` | 包外（可读 + 可执行 + **不可改**） |
| 测试数据说明 | `test/test-data/` | 包外（**不读**；关键已摘到 `02-基线快照.md`） |

---

## 六、Codex 自查命令

```bash
# 在 deliverables/<本包>.active/ 目录下执行（脚本待 generate-research-deliverable 自动生成）
./run-self-check.sh
# 内含：
#   1. cd test/tools/api-collection && bru run --env Local <模块>
#   2. cd test/tools/e2e-scripts && pnpm playwright test tests/regression/<模块>
# 仅跑本期 § 二 + § 三 中 auto:Y 的 case_id；§ 四 § 4.1 已作废 case_id 不跑
```

通过率要求：**`auto:Y` 用例 100% pass**；任何一条 fail 都要写 `08-修复历史.md` 说明。

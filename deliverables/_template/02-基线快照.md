# 02 · 基线快照（冻结）

> 🔧 项目无关骨架版 · 示例均为占位 · 项目专属配置见 PROJECT-PROFILE.md

> 本文件记录"包创建那一刻"的基线状态。**冻结后不允许更新**；上游基线变化要反映到本包，必须先 `.superseded` 再起新包。

---

## 一、基线版本

| 字段 | 值 |
|---|---|
| 基线编号 | `<B1.0.x>`（按 `product-docs/.../baseline/01-基线版本登记表.md`） |
| 基线发布时间 | `<YYYY-MM-DD>` |
| 关联 PRD 版本 | `<PRD 文档名称 + 版本号 + 链接（仅作出处标注，Codex 不要打开）>` |
| 关联差异台账快照 | 见 § 三 |
| 关联变更登记快照 | 见 § 四 |

---

## 二、代码冻结点

| 字段 | 值 |
|---|---|
| 仓库 | `<git 仓库地址>`（凭据已脱敏；见 PROJECT-PROFILE.md § 二） |
| 分支 | `<拉取分支，例 release/v*>`（见 PROJECT-PROFILE.md § 二） |
| Commit SHA | `<填写包创建时的 HEAD commit hash，40 位>` |
| Commit 时间 | `<YYYY-MM-DD HH:mm UTC+8>` |
| Commit 标题 | `<commit 标题>` |

> Codex 干活前请先确认 `cd code/<仓库名> && git rev-parse HEAD` 是否与上述 SHA 一致；不一致请在 `08-修复历史.md` 标 `[BASELINE-DRIFT]` 并联系 PM。

---

## 三、相关 DIFF 摘录

> 仅摘与本次需求相关的 DIFF 条目（PM 手动筛选）。**完整台账不放入此处**，避免 Codex 视野扩散。

| DIFF 编号 | 标题 | 状态 | 与本需求的关系 |
|---|---|---|---|
| `<DIFF-XXX>` | `<示例：代码仓库结构与说明文档不一致>` | `待裁决` | `<决定本次白名单路径怎么写>` |
| `<DIFF-YYY>` | `<...>` | `<已修复 / 待裁决 / 不修复>` | `<...>` |

---

## 四、相关 CHG 摘录

> 同 § 三，仅摘与本需求相关。

| CHG 编号 | 标题 | 状态 | 关系 |
|---|---|---|---|
| `<CHG-XXX>` | `<示例模块闭环>` | `已立项` | 本次的"父需求" |
| `<CHG-YYY>` | `<...>` | `<...>` | `<...>` |

---

## 五、本包不依赖（但需要知道存在）的上游事实

> 帮助 Codex 理解大背景，但**不要去读原文档**。

- `<B1.0.x>` 基线包含 `<N>` 个一级菜单：`<模块A / 模块B / ... / 示例模块>`（完整清单见 PRD / 领域术语 PROJECT-PROFILE.md § 四）。
- 本次需求只在 "<示例模块>" 内做改动。
- 项目技术栈 / 端结构按 `PROJECT-PROFILE.md § 五`（前端框架 / 构建工具 / 代码生成器 / 后端语言与构建链等）。

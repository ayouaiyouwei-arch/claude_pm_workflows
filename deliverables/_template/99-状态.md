# 99 · 状态（State Tracker）

> 🔧 项目无关骨架版 · 示例均为占位 · 项目专属配置见 PROJECT-PROFILE.md

> 本文件追踪本交付包的实时状态。**状态变更必须经过 `promote-deliverable` skill**，禁止手改本文件后强行改目录后缀。

---

## 一、当前状态

| 字段 | 值 |
|---|---|
| **当前状态** | `<draft / active / done / superseded / hotfix>` |
| **目录后缀** | `<.draft / .active / .done / .superseded / .hotfix>` |
| **最近变更时间** | `<YYYY-MM-DD HH:mm UTC+8>` |
| **变更人** | `<PM 姓名>` |
| **当前 owner** | `<姓名 / Codex>` |

---

## 二、状态变更历史

> 每次 `promote-deliverable` 自动追加一行；禁止手改。

| 序 | 时间 | 从 | 到 | 操作人 | 备注 |
|---|---|---|---|---|---|
| 1 | `<YYYY-MM-DD HH:mm>` | - | `.draft` | PM | 包创建 |
| 2 | `<YYYY-MM-DD HH:mm>` | `.draft` | `.active` | PM | 评审通过 |
| 3 | `<YYYY-MM-DD HH:mm>` | `.active` | `.done` | PM | 验收通过 |
| 4 | `<YYYY-MM-DD HH:mm>` | `.done` | `archive/` | PM | 冷却 7 天后归档 |

---

## 三、前置校验快照（最近一次 promote 时记录）

> 由 `promote-deliverable` skill 自动写入，PM 不需手填。

```
[promote 时间] 2026-05-15 18:00:00 UTC+8
[操作] .active → .done

✅ 全工作空间 .active 包数：1（本包）
✅ 06-验收标准.md § 七 9 项硬检查：9/9 已勾
✅ 08-修复历史.md 末尾轮次：[ROUND-3] 已 PM 确认
✅ 关联 <CHG-XXX> 状态：已上线
✅ <DIFF-XXX> 状态：（无关联）
```

---

## 四、违例锁定记录

> 出现违规（多 active / 跳过校验 / 强行改后缀）时由 `promote-deliverable` 锁定本包。

| 序 | 时间 | 违规描述 | 解锁人 | 解锁时间 |
|---|---|---|---|---|
| - | - | - | - | - |

---

## 五、关联资源

| 资源 | 路径 |
|---|---|
| 关联 CHG | `product-docs/.../baseline/03-产品变更登记.md#<CHG-XXX>` |
| 关联 DIFF（如有） | `product-docs/.../baseline/02-PRD-实现差异台账.md#<DIFF-XXX>` |
| 当前归属基线 | `<B1.0.x>`（升级到 `.done` 后回写到 `01-基线版本登记表.md`） |
| 测试执行记录 | `test/execution/<B1.0.x>-<本期>/执行清单.csv` |
| 测试缺陷记录 | `test/execution/<B1.0.x>-<本期>/缺陷清单.csv` |
| 验收报告 | `test/reports/<B1.0.x>-<本期>/验收报告.md` |

---

## 六、流水线指标快照（机器可读，retrospector 写入）

> 本段由 `pipeline-retrospector` 在 `.done` 时自动填充，PM 不必手填。
> 字段定义对齐 `evals/_runs字段说明.md` v1.0。**已填值后禁止手改**——纠错走 `evals/runs.csv` 追加新行。

```yaml
# === retrospect-snapshot v1 ===
类型: <新增 | 优化 | UI重构>
触及端: <admin;cockpit;...>
修改级别: <L1 | L2 | L3>
A1_轮数: <int>
A2_结论: <通过 | 打回 | 打回后通过>
A3_声明改动文件数: <int>
A4_结论: <通过 | 触发A5 | 打回>
A5_是否触发: <是 | 否>
A5_建议: <通过原方案 | 拆分 | 延后 | ->
A6_用例总数: <int>
A6_VR占比: <0.0~1.0 | ->
A7_打回轮数: <int>
Codex_轮次: <int>
Codex_QUESTION数: <int>
实际改动文件数: <int>
retrospect_时间: <YYYY-MM-DD HH:mm UTC+8>
retrospector_版本: <agent version>
# === retrospect-snapshot end ===
```

**核心模块清单**（写到 `knowledge/cases.csv § 6 核心模块`）：
- <module1>
- <module2>

**关键决策摘要**（≤ 80 字，写到 `knowledge/cases.csv § 7 关键决策摘要`）：
> <一两句话：本期最重要的产品 / 技术决策是什么>

**关联 patterns**（retrospector 匹配，PM 校正）：
- <P001;P003 或 ->


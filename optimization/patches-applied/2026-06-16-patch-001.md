# patch-001 · 2026-06-16

- **合并人**：PM（hyukminpark · 经 UPGRADE-PLAN-v1.1 roadmap 评审授权直接合并）
- **触发源**：`optimization/UPGRADE-PLAN-v1.1.md` Wave 1 ③b + Wave 2 ④⑤（roadmap 驱动 · 非 .done 包 retrospect）
- **来源补丁文件**：`optimization/patches-pending/2026-06-16-upgrade-wave1-2-experience-axis.md`（3 条:P-1/P-2/P-3）
- **改动文件**：
  - `.claude/agents/product-expert.md` **v1.6 → v1.7**（P-1 + P-2）
  - `.claude/agents/requirement-reviewer.md` **v1.2 → v1.3**（P-3）
  > ⚠️ 版本基线说明:合并前 `agent-versions.json` 记两者均为 `1.0`,但 agent 文件 frontmatter 实为 `1.6`/`1.2`（骨架自带 patch-009~014 烘焙内容与 json 初始化漂移）。本次按 **frontmatter 真实值** bump（1.6→1.7 / 1.2→1.3）并同步 json,消除这两个 agent 的漂移;其余 9 个 agent 的 json↔frontmatter 漂移未在本次范围内（见 §遗留）。

- **改动锚点（均经 LOCKED 校验 · 锚点段 byte-identical 未触碰）**：
  - P-1 · product-expert §「设计方法论库」映射表（表尾追加 3 行 · 第 99–109 行区,LOCKED-safe）
  - P-2 · product-expert §「输出文档结构 · 四 需求细化」新增 §4.8（置于 §4.7 后 · §五 前 · 无 anchor-LOCKED）
  - P-3 · requirement-reviewer §「第 11 项可用性软闸」操作步骤新增 1b（第 50 行后 · 在两个 A2 LOCKED 段之外）

- **改动内容**：
  ```diff
  # product-expert.md 映射表（P-1）
  + | 受众门控 audience=toC + 新增整页/新功能 | journey-map + onboarding-design | § 4.8 激活漏斗 + 劝退断崖 |
  + | 受众门控 audience=toC + 体验路径/留存类 | experience-map + metrics-definition | § 4.8 体验路径 + 漏斗指标 |
  + | 受众门控 audience=toB + 列表/批量/角色权限类 | information-architecture + navigation-patterns | § 4.8 角色→任务→完成效率 + 角色 IA |

  # product-expert.md 输出结构（P-2）
  + ### 4.8 用户体验路径 + 劝退点扫描（受众门控 · 仅 audience_profile.primary 命中时产）
  +   toC → 激活漏斗 + 流失断崖 + 逐点缓解;toB → 任务流程 + 效率摩擦 + 角色 IA;both → 按 surface 分画

  # requirement-reviewer.md 第 11 项软闸（P-3）
  + 1b.（受众门控 · 劝退风险 lens）toC → drop-off severity lens;toB → 效率/可学习性 lens
  +    严重度 ≥3 同档升 PM（两选处置:回炉 / 带备注放行）· 软闸不参与打回
  ```

- **触发理由**：实测 A1 有 jobs-to-be-done/IA/Nielsen 但无任何环节显式产"体验路径 + 劝退点";根因 = journey-map/experience-map 等卡片未进快照、未接 A1。Wave 1 已 cp 4 张卡（journey-map/experience-map/onboarding-design/metrics-definition）入 `knowledge/methodology/`,本 patch 把它们接进 A1 产物 + A2 软闸,闭环回答 PM "会不会劝退"。
- **回归结果**：⚠️ **PM 显式选择"B. 跳过回归"** —— `evals/regression-set/cases.csv` 仅表头（A2/A4/A7 回归闸未上膛 · 骨架未跑过真实 run）。按 optimize-prompts 第 0 步授权:本次合并后续要**人工抽查**。LOCKED 锚点校验已过（两 agent LOCKED 段 byte-identical）。
- **行为验收口径（替代回归 · 待首个 toC/toB 需求触发）**：toC 需求 → A1 出 §4.8 激活漏斗 + 劝退断崖、A2 软闸把高 drop-off 升 PM;toB 需求 → A1 出任务流程 + 效率摩擦;**web/无 audience 项目不触发任何受众卡**（门控未命中即跳过）。
- **关联**：`optimization/UPGRADE-PLAN-v1.1.md` · `knowledge/methodology/README.md`（34 卡清单）· `PROJECT-PROFILE.md §5.1` 双轴字段。
- **回滚点**：`optimization/regression-baseline/product-expert-v1.6/` + `requirement-reviewer-v1.2/`（pre-change 快照 · `cp` 回原位即回滚 + 还原 agent-versions.json）。

## 遗留（非本次范围）
- 其余 9 个 agent 的 `agent-versions.json`（均记 1.0）与各自 frontmatter 可能存在同类漂移,建议某次月更统一校准。
- `evals/regression-set/cases.csv` 填充 = UPGRADE Wave 1 ⓪,阻塞于"无真实 run 数据";有首条 run 后补 ≥2 个 A2 case,本 patch 的 P-3 转人工抽查→回归覆盖。

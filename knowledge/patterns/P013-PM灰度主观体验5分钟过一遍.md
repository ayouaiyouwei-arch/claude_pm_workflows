---
模式编号: P013
标题: PM 灰度主观体验 5 分钟过一遍（dev-verify 自动化 smoke 之外的必要补充）
首次发现: 2026-05-27
出现次数: 1（实战元教训）
最近出现: 2026-05-27
关联agent: -（流水线外 · PM 自身机制）
状态: active（默认执行 · 与 P005 dev-verify smoke 并列）
---

# P013 · PM 灰度主观体验 5 分钟过一遍

## 描述

任何包升 `.done` 后 · 即使 P005 dev-verify 自动化 smoke 通过 + 全量回归 100% PASS + 0 blocker · PM **仍必须亲自在 dev 灰度环境用 5 分钟主观体验过一遍核心场景**。理由：

1. **自动化覆盖 ≠ PM 期望覆盖**：用例描述"P0 P1 P2"等机械维度 · 不覆盖 PM "看了爽不爽 / 体验顺不顺" 的主观维度
2. **PRD / 用例无法穷举主观判断**：如"圆角好不好看" / "切换有没有视觉反馈" / "整页是否被遮挡" · PM 看一眼就知道 · 写文档反而难
3. **PM 灰度实拍能产新 OPT 需求**：实战案例 · 自动化 100% PASS · 但 PM 实拍发现 4 个新问题 · 触发 followup 包 · 这是流水线"自进化"的根本输入

## 触发场景

任一 `.done` 包升级后 **必触发**（与 P005 dev-verify 并列默认动作）：

- /new-feature 第 9 步 retrospect 完成后
- promote-deliverable B-后置 .draft → .done 后
- 手动追认 .done 完成后

## 5 分钟过一遍清单（PM 自检脚本）

```
□ 1. 打开本期触及的主页面（按 99-状态 § 五 触及端列表）· 每端 1 分钟主流程过一遍
□ 2. 切换 2 个常见过滤态 / 模式态 · 看视觉反馈是否符合 PM 期望
□ 3. 小屏（如 1366×768）+ 大屏（如 1920×1080）两个浏览器窗口分别开一次 · 看右栏 / 弹层 / 浮动元素是否截断
□ 4. 触发 1 个边界态（如空数据 / 网络错误 / 无权限）· 看 UI 文案 / 颜色 / 占位是否得体
□ 5. 写"PM 主观体验摘要"（≤ 50 字）到 99-状态 § 五 验收痕迹 · 含 ✅ / ⚠️ / 🚨 三档：
   - ✅ 体验顺畅 · 无新问题
   - ⚠️ 发现轻微问题（写问题描述 + 下次跟踪）· 不开新包
   - 🚨 发现 P0 阻断 · 立即开 followup 修复包
```

### 地图/canvas 类需求 · 固定动作清单（P020 LOCKED 补充 · 命中地图/图表必跑）

> 地图折线/站点/聚焦/缩放这类东西自动化测不全（canvas 盲区），PM 灰度必须按下面固定动作手动过一遍：

```
□ M1. 选 1 个已建好的对象（线路/区域等）→ 看地图是否同时画出【点位 + 连接它们的折线】（不能只有点没有线）
□ M2. 选中后 → 看地图镜头是否【自动聚焦/推到】目标范围（不是停在全局全景）
□ M3. 滚轮缩放 + 鼠标拖拽地图 → 手感是否【流畅跟手】，不卡顿不漏响应
□ M4. 切到另一个对象 / 切回"全部" → 看 overlay 是否正确重绘、无残留
□ M5. 选 1 个【无关联实时数据】的对象 → 确认 overlay/聚焦照常（无数据不应导致空白或不聚焦）
```

> 命中任一异常 → 按 ✅/⚠️/🚨 三档写入 99-状态 § 五，🚨 立即开 followup。

## 与 P005 dev-verify 的关系

| 维度 | P005 dev-verify | P013 PM 5 分钟体验 |
|---|---|---|
| 执行者 | 自动化（Playwright + 截图脚本）| PM 本人（人肉浏览器）|
| 时长 | 30s~2min | 5 分钟 |
| 覆盖 | 元素存在性 + 接口响应 + 截图归档 | 主观体验 + 视觉契约 + 边界态 |
| 输出 | evidence/dev-verify-*.json + 截图 | 99-状态 § 五 "PM 主观体验摘要" + 必要时新 followup 包 |
| 关系 | **并列**（不替代）· P013 比 P005 更高层 · 但 P005 比 P013 更频繁可重复 |

## 已采取的对策

| 改动 | 改动文件 | 改动锚点 |
|---|---|---|
| 沉淀 P013 LOCKED | `knowledge/patterns/P013-PM灰度主观体验5分钟过一遍.md`（本文件）| - |
| 加入 CLAUDE.md 关键约束 | `CLAUDE.md` § 关键约束 | 新增第 8 项（建议）|

## 残余风险 / 仍未解决的子情况

- PM 可能因忙跳过 5 分钟体验 · 直接信任 dev-verify 通过 → 主对话应在 `.done` 升级后**主动提醒** PM "建议跑 P013 5 分钟体验"（不强制 · 主对话不能阻断 PM 时间）
- 5 分钟可能不够 cover 复杂大需求 · PM 自行延长（大屏类需求建议 10~15 分钟）

## 升格 / 降格条件

- **保持 active**：连续 5 个 `.done` 包 PM 都执行了 P013 · 至少 1 次发现新问题（说明机制真有效）
- **降格为已规避**：连续 10 个 `.done` 包 PM 都执行 P013 · 0 次发现新问题（罕见 · 说明流水线已足够成熟）
- **不会废弃**（PM 主观体验是流水线"自进化"的根本输入 · 不可被自动化完全替代）

## 关联资源

- 相关：P005（dev-verify 自动化）+ P009（洋葱剥皮 bug 修复链）
